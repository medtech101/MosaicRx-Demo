export function PrivacyBanner() {
  return (
    <div className="privacy-banner" role="note">
      🔒 Personal study use only. Nothing here is ever shared, published, or sent to any third
      party except the configured LLM API used for summarization/flashcards.
    </div>
  );
}
